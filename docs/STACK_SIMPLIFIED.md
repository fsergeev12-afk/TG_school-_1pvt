# Упрощенный стек для локальной разработки

## ✅ Что используем

### Backend
- ✅ **NestJS (TypeScript)** - модульная архитектура
- ✅ **SQLite** - файловая БД (вместо PostgreSQL)
- ✅ **TypeORM** - ORM (поддерживает и SQLite, и PostgreSQL)
- ❌ **Без Redis** - уведомления через простой setTimeout
- ❌ **Без Bull Queue** - пока не нужны очереди
- ✅ **node-telegram-bot-api** - Telegram Bot API

### Frontend
- ✅ **React 18 + TypeScript**
- ✅ **Vite** - быстрая сборка
- ✅ **Telegram WebApp SDK** - интеграция с Telegram
- ✅ **TanStack Query** - state management
- ✅ **Tailwind CSS** - Mobile-First UI

### Infrastructure
- ❌ **Без Docker** - всё локально на твоем компе
- ✅ **Git** - версионирование
- ✅ **ngrok** - туннель для тестирования на мобильных (опционально)

## 🔄 Легкий переход на продакшн стек

### Когда будешь готов к деплою:

#### 1. PostgreSQL вместо SQLite

**Было (`.env`):**
```bash
DATABASE_TYPE=sqlite
DATABASE_NAME=database.sqlite
```

**Стало:**
```bash
DATABASE_TYPE=postgres
DATABASE_HOST=your_postgres_host
DATABASE_PORT=5432
DATABASE_USER=your_user
DATABASE_PASSWORD=your_password
DATABASE_NAME=telegram_course_platform
```

TypeORM автоматически переключится на PostgreSQL!

#### 2. Redis для Bull Queue (опционально)

**Установить Redis:**
- Локально через WSL: `sudo apt install redis-server`
- Или облачный: Upstash (бесплатный tier)

**Обновить код:**
```typescript
// Вместо setTimeout
await this.notificationQueue.add('send-demo', {
  userId,
  streamId,
}, {
  delay: 10000 // 10 секунд
});
```

#### 3. File Storage

**Сейчас:** Локальные файлы (временно)
**Потом:** Telegram File Storage через Bot API (уже готово в коде)

## 🎯 Преимущества упрощенного стека

### Для разработки:
- ✅ Не нужно устанавливать PostgreSQL
- ✅ Не нужно устанавливать Redis
- ✅ Не нужно настраивать Docker
- ✅ Всё работает "из коробки"
- ✅ Быстрый старт (npm install → npm run dev)

### Для продакшена:
- ✅ Архитектура готова к масштабированию
- ✅ БД легко мигрируется (SQLite → PostgreSQL)
- ✅ Можно добавить Redis без переписывания
- ✅ Код не меняется (только конфигурация)

## 📊 Сравнение стеков

| Компонент | Разработка | Продакшн |
|-----------|------------|----------|
| БД | SQLite (файл) | PostgreSQL (облако) |
| Cache/Queue | Без Redis | Redis (Upstash) |
| Backend Host | localhost:3000 | Railway/Render |
| Frontend Host | localhost:5173 | Vercel/Netlify |
| File Storage | Локально | Telegram Bot API |
| Webhook | ngrok (тест) | Production URL |

## 🚀 Миграция на продакшн (3 простых шага)

### Шаг 1: Настроить БД

```bash
# Экспорт данных из SQLite
sqlite3 database.sqlite .dump > backup.sql

# Импорт в PostgreSQL
psql -U your_user -d telegram_course_platform < backup.sql
```

### Шаг 2: Обновить .env

```bash
# Backend
DATABASE_TYPE=postgres
DATABASE_HOST=your_postgres_host
...

# Frontend
VITE_API_URL=https://your-backend-url.com/api
```

### Шаг 3: Деплой

```bash
# Backend → Railway/Render
git push origin main

# Frontend → Vercel
vercel --prod
```

Готово! ✅

## 📝 Что НЕ изменилось

- ✅ Архитектура БД (та же самая)
- ✅ Все Entity модели (те же самые)
- ✅ API endpoints (те же самые)
- ✅ Frontend компоненты (те же самые)
- ✅ Бизнес-логика (та же самая)

**Меняется только конфигурация!**

## 🎓 Выводы

Упрощенный стек позволяет:
1. **Быстро начать** разработку (без сложной настройки)
2. **Тестировать локально** (всё на твоем компе)
3. **Легко мигрировать** на продакшн (меняем .env)
4. **Сохранить архитектуру** (готова к масштабированию)

**Идеально для MVP!** 🚀



